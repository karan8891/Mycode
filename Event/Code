from airflow import DAG
from airflow.operators.python import PythonOperator
from airflow.utils.dates import days_ago

def list_google_sts_operators():
    from airflow.providers.google.cloud.operators import cloud_storage_transfer_service
    print("Available operators in cloud_storage_transfer_service module:")
    print(dir(cloud_storage_transfer_service))

with DAG(
    dag_id="debug_check_google_sts_operators",
    start_date=days_ago(1),
    schedule_interval=None,
    catchup=False
) as dag:

    check_ops = PythonOperator(
        task_id="list_sts_operators",
        python_callable=list_google_sts_operators
    )
