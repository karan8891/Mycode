resource "google_pubsub_subscription" "cf_trigger" {
  name  = "gdw-cf-trigger"
  topic = google_pubsub_topic.sts_completion_topic.name
  push_config {
    push_endpoint = "https://..."
  }
}

resource "google_pubsub_subscription" "dag_trigger" {
  name  = "sts-completion-sub"
  topic = google_pubsub_topic.sts_completion_topic.name
}
